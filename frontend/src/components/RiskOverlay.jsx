/**
 * RiskOverlay.jsx — Color-coded flood risk layer
 *
 * Renders choropleth polygons on the map based on simulation API results.
 * Colors:
 *   - Safe: #22c55e (green)
 *   - Low risk: #eab308 (yellow)
 *   - Medium risk: #f97316 (orange)
 *   - High risk: #ef4444 (red)
 *   - Extreme risk: #7f1d1d (dark red)
 */

import React from 'react';

function RiskOverlay() {
  return null; // Renders as a map layer — to be implemented in Phase 3
}

export default RiskOverlay;
